InfoNCEParameters:
  GlobalParams:
    contrast_on: [ partition, patient, cycle ]
  DenseParams:
    include_all: true
  LossParams:
    temperature: 0.07
  weight: 0.1
Optim:
  pre_lr: null
  ft_lr: null
ProjectorParams:
  GlobalParams:
    feature_names: [ Conv5, Conv5, Conv5 ]
    feature_importance: [ 1, 0.5, 0.1 ]
    head_type: mlp
    normalize: true
    pool_name: adaptive_avg # adaptive_max #identical


  DenseParams:
    feature_names: null
    feature_importance: [ ]
    head_type: mlp
    normalize: false # in order to perform pooling manually`1
    pool_name: identical # adaptive_max #identical
    spatial_size: 16
    pool_method: adaptive_avg
